refresh_rate: 30 seconds
appenders:
  stdout:
    kind: console
    encoder:
      pattern: "{d(%y/%m/%d %H:%M:%S)} {l} {f}: {m}{n}"
  file:
    kind: file
    path: "/tmp/boson.log"
    encoder:
      pattern: "{d(%y/%m/%d %H:%M:%S)} {l} {f}: {m}{n}"
root:
  level: info
  appenders:
    - stdout
